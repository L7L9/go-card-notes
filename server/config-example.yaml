#数据库配置
mysql:
  username:
  password:
  host:
  port: 3306
  db-name:
  max-open-conn: 100
  max-idle-conn: 20

#日志配置
zap:
  level: info
  director: log
  log-in-console: true

#jwt配置
jwt:
  signing-key:
  issuer:
  #以秒为单位
  expires-time: 3600

#system配置
system:
  addr: 8888
  admin-account: admin
  admin-password:
  #记得加斜杠(如果要设置前缀的话)
  router-prefix: ""

#ipfs配置
ipfs:
  host:
  port: